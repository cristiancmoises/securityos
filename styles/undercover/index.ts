import type { DefaultTheme } from "styled-components";
import colors from "styles/undercover/colors";
import formats from "styles/undercover/formats";
import sizes from "styles/undercover/sizes";

const undercover: DefaultTheme = {
  colors,
  formats,
  name: "Undercover",
  sizes,
};

export default undercover;
